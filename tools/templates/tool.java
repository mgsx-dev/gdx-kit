package ${package}.tools;

import net.mgsx.game.core.EditorScreen;
import net.mgsx.game.core.tools.Tool;

public class ${classname} extends Tool {

    public ${classname}(EditorScreen editor) {
        super("${name}", editor);
    }

    @Override
    protected void activate() 
    {
        // TODO
    }
}
